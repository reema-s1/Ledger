/**
 * There's no auth system in this project (documented simplification,
 * consistent with Section 6's `user_id` query param). The frontend
 * operates as this one fixed demo user; `npm run seed-demo-user` ensures
 * it exists with a starter watchlist.
 */
export const DEMO_USER_ID = 1;
